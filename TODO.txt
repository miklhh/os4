1.  - Work on the tasking.
        - Implement a fork function?
        - Implement a exec function?
        - Test for stability in current implementation.
        - Clean up old code.
        - Pimp the current code...
        - Look at 'pit-irq-wrapper', do we really need to 'pusha' and 'popa'?
    - Implement loging system.
    - Complete FAT32 filesystem.
    - Implement a kernel stack smash protector.
	- Log all the interrupt handlers.
	- Keyboard driver needs more support.
    - Implement higher half kernel.

2.  - Remove -g flag on release (so that debug symbols wont be avaliable in binary).
